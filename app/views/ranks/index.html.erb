<h2>投票結果</h2>

<%= Time.zone.now %>
現在

<div class="container">
  <table>
    <% @songs.each_with_index do |song, i| %>
      <tr>
        <td> <b><%= i + 1 %></b> </td>
        <td> <%= link_to song.title, edit_vote_path(id: song.to_param) %> </td>
        <td> <%= song.votes_count %> </td>
      </tr>
    <% end %>
  </table>
</div>

<a href="/">戻る</a>
